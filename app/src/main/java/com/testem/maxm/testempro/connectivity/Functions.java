package com.testem.maxm.testempro.connectivity;

public enum Functions {
    AUTHENTIFIER_CACHE,      //used when authentication completes with cache file
    AUTHENTIFIER,            //used when authentication completes with sign in
    REPORT_END,              //used when session is ended
    VIEWER,
    EDITOR,
    CHECKER
}
